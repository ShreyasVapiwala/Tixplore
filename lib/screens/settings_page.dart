import 'package:flutter/material.dart';

class SettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Settings And Privacy'),
      ),
      body: Center(
        child: Text('Settings And Privacy Page'),
      ),
    );
  }
}
